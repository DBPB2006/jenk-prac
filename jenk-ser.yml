apiVersion: v1
kind: Service

metadata:
  name: jenk-service
  labels:
    app: jenk-app

spec:
  selector:
    app: jenk-app

  ports:
    - protocol: TCP
      port: 4567
      targetPort: 80

  type: ClusterIP